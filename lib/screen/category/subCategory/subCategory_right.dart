import 'package:flutter/material.dart';

class SubCategoryRight extends StatelessWidget {
  const SubCategoryRight({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
